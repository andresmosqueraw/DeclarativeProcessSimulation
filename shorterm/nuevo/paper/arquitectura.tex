\documentclass[a4paper]{article}

\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, shapes, calc, fit}
\usepackage{float}
\usepackage{geometry}
\geometry{margin=1cm, landscape}

\begin{document}

\title{System Overview: Nuevo Pipeline Architecture (with What-If)}
\author{}
\date{}
\maketitle

\section{System Overview}

This diagram illustrates the complete architecture of the \texttt{nuevo} pipeline, including the AS-IS baseline simulation and the Declarative What-If analysis using partial state.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    scale=0.68,
    transform shape,
    node distance=0.8cm and 1.5cm,
    script/.style={rectangle, rounded corners, draw=black, thick, 
                   minimum width=3.2cm, minimum height=1.6cm, 
                   text width=3cm, align=center, font=\small\bfseries},
    procstep/.style={rectangle, rounded corners, draw=black, fill=gray!10,
                 minimum width=2.5cm, minimum height=0.6cm,
                 text width=2.3cm, align=center, font=\tiny},
    input/.style={rectangle, draw=blue!60, fill=blue!15, 
                  minimum width=1.8cm, minimum height=0.5cm, 
                  text width=1.6cm, align=center, font=\tiny},
    output/.style={rectangle, draw=green!60, fill=green!15, 
                   minimum width=1.8cm, minimum height=0.5cm, 
                   text width=1.6cm, align=center, font=\tiny},
    config/.style={rectangle, draw=purple!60, fill=purple!15, 
                   minimum width=1.8cm, minimum height=0.5cm, 
                   text width=1.6cm, align=center, font=\tiny},
    tool/.style={rectangle, draw=orange!60, fill=orange!15,
                 minimum width=2cm, minimum height=0.6cm,
                 text width=1.8cm, align=center, font=\tiny},
    arrow/.style={->, >=stealth, semithick},
    phase/.style={rectangle, draw=gray!40, fill=gray!8, 
                  minimum width=3cm, minimum height=0.5cm, 
                  font=\tiny\bfseries, align=center},
    internal/.style={->, >=stealth, thin, gray!60}
  ]
    
    % Phase labels at top
    \node[phase] (phase1) at (-13, 9) {PHASE 1: BPMN/JSON EXTRACTION};
    \node[phase] (phase2a) at (-5, 9) {PHASE 2A: STATE};
    \node[phase] (phase2b) at (3, 9) {PHASE 2B: SIMULATION (AS-IS)};
    \node[phase] (phase2c) at (12, 9) {PHASE 2C: WHAT-IF (TO-BE)};
    
    % ========== PHASE 1: AS-IS MODEL ==========
    
    \node[input] (log) at (-17, 6.5) {Log\\\texttt{*.csv}};
    
    \node[script, fill=blue!25] (extract) at (-13, 6) {
      \texttt{extract\_bpmn\_json.py}
    };
    
    \node[tool] (simod1) at (-13, 3.5) {Simod (AS-IS)};
    
    \node[output] (bpmn-asis) at (-14.5, 1) {AS-IS\\\texttt{*.bpmn}};
    \node[output] (json-asis) at (-11.5, 1) {AS-IS\\\texttt{*.json}};
    \node[font=\tiny] (simod-dir) at (-13, 0.5) {\texttt{data/generado-simod/}};

    % Caja contenedora (Folder)
    \node[draw=gray!80, rounded corners, inner sep=0.2cm, fit=(bpmn-asis) (json-asis) (simod-dir)] (simod-box) {};
    
    \draw[arrow, blue!70] (log.east) -- (extract.west);
    \draw[internal] (extract.south) -- (simod1.north);
    \draw[arrow, gray!70] (simod1.south) -- (bpmn-asis.north);
    \draw[arrow, gray!70] (simod1.south) -- (json-asis.north);
    
    % ========== PHASE 2A: STATE ==========
    
    \node[script, fill=cyan!25] (compute) at (-5, 6) {
      \texttt{compute\_state.py}
    };
    
    \node[procstep] (cut-strategy) at (-5, 4) {Strategy:\\fixed/wip3/seg10};
    \node[procstep] (calc-state) at (-5, 2.5) {Calc State\\(N-Gram)};
    
    \node[output] (states) at (-5, 1) {States\\\texttt{*\_state\_*.json}};
    \node[font=\tiny] at (-5, 0.5) {\texttt{data/generado-state/}};
    
    \draw[arrow, blue!70, dashed] (log.north) -- ++(0,0.5) -| (compute.north);
    %\draw[arrow, blue!80] (bpmn-asis.east) -- ++(0.5,0) |- (-10,2) |- (compute.west);
    %\draw[arrow, purple!80] (json-asis.east) -- ++(0.2,0) |- (compute.west);
    % Conexión desde la derecha (.east) de la caja hacia compute
    \draw[arrow] (simod-box.east) -- ++(0.5,0) |- (compute.west);
    
    \draw[internal] (compute.south) -- (cut-strategy.north);
    \draw[internal] (cut-strategy.south) -- (calc-state.north);
    \draw[arrow, green!70] (calc-state.south) -- (states.north);
    
    % ========== PHASE 2B: SIMULATION (AS-IS) ==========
    
    \node[script, fill=orange!25] (sim-asis) at (3, 6) {
      \texttt{run\_simulation.py}
    };
    
    \node[tool] (prosimos-asis) at (3, 3.5) {Prosimos\\(Short-Term)};
    
    \node[output] (out-asis) at (3, 1) {Results\\\texttt{punto-corte-X/}};
    \node[font=\tiny] at (3, 0.5) {\texttt{data/generado-short-term/}};
    
    % Inputs for Phase 2B
    \draw[arrow, green!70, thick] (states.east) -- ++(1.5,0) |- (3, 7.5) -- (sim-asis.north);
    %\draw[arrow, blue!80] (bpmn-asis.south) -- ++(0,-0.5) -| (sim-asis.south); % Long arrow around
    %\draw[arrow, purple!80] (json-asis.south) -- ++(0,-0.3) -| (sim-asis.south);
    % Conexión desde la derecha (.east) de la caja hacia compute
    \draw[arrow] (simod-box.east) -| (-9,0) -- ++(10,0) |- (sim-asis.west);
    
    \draw[internal] (sim-asis.south) -- (prosimos-asis.north);
    \draw[arrow, green!70] (prosimos-asis.south) -- (out-asis.north);
    
    % ========== PHASE 2C: WHAT-IF (TO-BE) ==========
    
    \node[script, fill=purple!25] (whatif) at (12, 6) {
      \texttt{run\_whatif\_\\declarative.py}
    };
    
    \node[input] (rules) at (17, 6) {Rules\\\texttt{rules.ini}};
    
    % Internal Steps What-If
    \node[procstep, fill=purple!10] (gen-traces) at (9, 4) {1. Deep Learn\\Synt. Traces};
    \node[procstep, fill=orange!10] (simod-tobe) at (12, 4) {2. Simod\\(TO-BE)};
    \node[procstep, fill=purple!10] (map-state) at (15, 4) {3. Map State\\AS-IS $\to$ TO-BE};
    
    \node[tool] (prosimos-tobe) at (12, 2.5) {Prosimos\\(Short-Term)};
    
    \node[output] (out-tobe) at (12, 1) {Results\\\texttt{whatif-X/cut-Y/}};
    \node[font=\tiny] at (12, 0.5) {\texttt{data/generado-whatif/}};
    
    % Inputs for Phase 2C
    \draw[arrow, purple!70] (rules.west) -- (whatif.east);
    \draw[arrow, green!70, thick] (states.east) -- ++(1.5,0) |- (10, 7.5) -| (whatif.north); % State input
    \draw[arrow, blue!80] (bpmn-asis.south) -- ++(0,-1) -| (17, 4) -- (map-state.east); % AS-IS for mapping
    
    % Internal Flow What-If
    \draw[internal] (whatif.south) -- (gen-traces.north);
    \draw[internal] (whatif.south) -- (simod-tobe.north);
    \draw[internal] (whatif.south) -- (map-state.north);
    
    \draw[internal] (gen-traces.south) |- (prosimos-tobe.west); % Traces -> Model -> Sim
    \draw[internal] (simod-tobe.south) -- (prosimos-tobe.north); % TO-BE Model
    \draw[internal] (map-state.south) |- (prosimos-tobe.east); % Mapped State
    
    \draw[arrow, green!70] (prosimos-tobe.south) -- (out-tobe.north);
    
    % ========== LEGEND ==========
    \node[anchor=north west, font=\tiny] at (-17, 0) {
      \begin{tabular}{@{}l@{}}
        \textcolor{blue!70}{$\rightarrow$} Input Data\\
        \textcolor{green!70}{$\rightarrow$} State/Results\\
        \textcolor{orange!70}{$\rightarrow$} Tool Exec
      \end{tabular}
    };
    
  \end{tikzpicture}
  \caption{Updated Architecture. \textbf{Phase 1}: AS-IS Model Discovery. \textbf{Phase 2A}: Partial State Computation (supports multiple cut-off strategies). \textbf{Phase 2B}: Baseline Short-Term Simulation using AS-IS model and partial state. \textbf{Phase 2C}: Declarative What-If Analysis. This phase (1) generates synthetic traces complying with declarative rules, (2) discovers a TO-BE BPMN model, (3) \textbf{maps the AS-IS partial state to the TO-BE model} (resolving ID mismatches), and (4) executes short-term simulation from the mapped state for ongoing cases.}
  \label{fig:architecture}
\end{figure}